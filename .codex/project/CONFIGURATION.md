# Configuration

## Detected Configuration Files
- `package.json` for validation script metadata.
- `.github/workflows/validate.yml` for CI validation.

## Configuration Sources
No runtime configuration sources are currently required.

## Precedence Rules If Known
Not applicable until runtime configuration exists.

## Validation Rules If Known
`scripts/validate-framework.js` validates required Markdown structure.

## Unknowns Requiring Human Review
- Whether consumers need configurable validation rules: Unknown - requires human input.
