# Data Model

## Detected Data Structures
- Markdown governance documents.
- Skills as directories containing `SKILL.md` and `CHECKLIST.md`.
- Templates as Markdown files with required metadata and review sections.

## Persistence Formats If Any
Markdown files in git.

## Schema Rules If Any
Required headings are enforced for framework docs, skills, checklists, and templates.

## Validation Rules If Any
`npm run validate` checks presence, non-empty content, and required headings.

## Unknowns Requiring Human Review
- Future machine-readable schema needs: Unknown - requires human input.
